node_modules
.next
src/generated
# 手元の型検査が書き換える中間ファイル。ビルドには不要なうえ、これが混ざると
# 型検査を走らせるたびに COPY . . の層が変わる (docs/80-デプロイ再高速化計画.md)
tsconfig.tsbuildinfo
# ノートの git 履歴 (開発時の実データ。docs/57)。builder の COPY . . で
# ビルドコンテキスト・中間レイヤーに取り込まないため
data
backup
.env
.env.*
!.env.example
.git
Dockerfile
compose.yaml
Caddyfile
README.md
